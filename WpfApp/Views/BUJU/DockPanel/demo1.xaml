<Window x:Class="WpfApp.Views.BUJU.DockPanel.demo1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:WpfApp.Views.BUJU.DockPanel"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        Title="DockPanel面板"
        Width="800"
        Height="450"
        mc:Ignorable="d">
    <!--默认情况下，后添加的元素只能使用剩余空间，无论对DockPanel的最后一个子元素设置任何停靠值，该子元素都将始终填满剩余的空间。
    如果不希望最后一个元素填充剩余区域，可以将DockPanel属性LastChildFill设置为false，还必须为最后一个子元素显式指定停靠方向。-->
    <DockPanel>
        <Button Content="ButtonLeft" DockPanel.Dock="Left" />
        <Button Content="ButtonTop" DockPanel.Dock="Top" />
        <Button Content="ButtonRight" DockPanel.Dock="Right" />
        <Button Content="ButtonBottom" DockPanel.Dock="Bottom" />
        <Button Content="ButtonTop" />
    </DockPanel>
</Window>

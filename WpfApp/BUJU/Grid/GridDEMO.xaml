<Window x:Class="WpfApp.BUJU.Grid.GridDEMO"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:WpfApp.BUJU"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        Title="Grid布局demo"
        Width="800"
        Height="450"
        mc:Ignorable="d">
    <Grid>
        <!--  3行2列的表格  -->
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <!--  定义元素，并指定单元格
        Grid.Row Grid.Column 不指定就是默认第0行第0列
        -->
        <TextBlock Grid.Row="0" Grid.Column="0">第一行第一列</TextBlock>
        <TextBlock Grid.Row="1" Grid.Column="0">第二行第一列</TextBlock>
        <TextBlock Grid.Row="2" Grid.Column="0">第三行第一列</TextBlock>
        <Button Grid.Row="0" Grid.Column="1">第一行第二列</Button>
        <Button Grid.Row="1" Grid.Column="1">第二行第二列</Button>
        <Button Grid.Row="2" Grid.Column="1">第三行第二列</Button>
    </Grid>
</Window>
